# Design Plan

This project ships an internationally planned design system — the style,
color language, and target market were decided before a single page was built.

- **Project**: rest
- **Brand**: Rest
- **UI style**: glassmorphism
- **Target market**: gcc — Gulf luxury: gold + emerald, deep neutral footer
- **Design Vault source**: built-in wheel
- **Display font**: Poppins
- **Body font**: Roboto

## Color system

| Token          | Value     |
| -------------- | --------- |
| `--t-primary`  | `#b8860b` |
| `--t-accent`   | `#0f766e` |
| `--t-heading`  | `#5b3200` |
| `--t-navBg`    | `#f8fafc` |
| `--t-footerBg` | `#0f172a` |

See `frontend/src/styles/design-tokens.json` for the machine-readable tokens.
